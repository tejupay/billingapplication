# Copy this file to .env.local for local development
# For production, set VITE_API_URL in your Vercel dashboard environment variables

# Local development (default — no need to change)
VITE_API_URL=http://localhost:8080

# Production (replace with your Render.com backend URL after deployment)
# VITE_API_URL=https://tejupay-backend.onrender.com
